<!--
THIS IS VERY IMPORTANT!!!
==========================================
AUTHOR: M. SZUL
AI MODEL: Claude Opus 5
TIMESTAMP: 2026-09-01 08:20:00
REASON FOR CREATION: Evidence wgrania host-guard z pula 192.168.2.0/24 na CBMS, zgodnie z zastana brama DARKSTAR-WiFi.
==========================================
-->

# Host guard — dopasowanie do 192.168.2.0/24

Data: 2026-09-01 ~08:17–08:20 UTC. Host: CBMS (`owner@100.71.8.70`).
Zrodlo: `deploy/firewall/host-guard.nft` z commita `1fb7ebe`.
Zakres: wylacznie plik regul i przeladowanie tabeli. **Bez** `nmcli`, bez nowego profilu,
bez rebootu, bez trybu hotspot.

## Roznica przed nadpisaniem

`diff -u` zywego pliku wobec nowego pokazal dokladnie dwie zmiany — nic wiecej:

~~~diff
-# TIMESTAMP: 2026-08-29 00:00:00
+# TIMESTAMP: 2026-09-01 08:10:27
-        elements = { 10.44.0.0/24 }
+        elements = { 192.168.2.0/24 }
~~~

## Kopia zapasowa

`/var/backups/darkstar-firewall/host-guard.nft.20260901` — `root:root`, 2274 B,
oryginal z 2026-08-29 18:51, SHA-256 `f5eb36c9228a6e0596efc6556bd566c2e03db90a6155d40b9b46806a2fdf30d7`.

Powrot: `sudo install -o root -g root -m 0644 /var/backups/darkstar-firewall/host-guard.nft.20260901 /etc/darkstar/host-guard.nft && sudo /usr/local/sbin/darkstar-firewall-apply`

## Instalacja i przeladowanie

| Krok | Wynik |
|---|---|
| `install -m 0644` do `/etc/darkstar/host-guard.nft` | `INSTALLED_OK`, SHA-256 `587df45de0172eb6ffc9c175e0bb850481d6addb3242c4373f321ff783612a47` |
| `darkstar-firewall-apply` | `APPLY=0`, `loaded inet darkstar_host_guard` |
| `darkstar-firewall-verify` | `host guard checks passed`, `VERIFY=0` |
| `nft list set inet darkstar_host_guard darkstar_downstream_ipv4` | `elements = { 192.168.2.0/24 }` |

Set zawiera **wylacznie** `192.168.2.0/24`; `10.44.0.0/24` zniknelo z zywej tabeli.

## Stan po zmianie

| Pomiar | Wynik |
|---|---|
| Trasa domyslna | `default via 192.168.1.1 dev wlp2s0` — uplink nietkniety |
| `DARKSTAR-WiFi` | `enp1s0:activated` — brama nie zostala przerwana |
| `wlp2s0`, `tailscale0` | `activated` — droga zarzadzania zyje |
| `enp1s0` adres | `192.168.2.1/24` |
| DNS downstream | `192.168.2.1:53` nadal nasluchuje |
| `darkstar-firewall.service` | `active` / `enabled` |

## Co to zmienia

Do tej pory zapora przepuszczala na zewnatrz wylacznie zrodla z `10.44.0.0/24`, a brama rozdawala
adresy z `192.168.2.0/24` — czyli klient dostawal adres, ale jego ruch nie mial reguly wyjscia.
Po tej zmianie regula `forward` i `masquerade` obejmuja pule, ktora brama faktycznie wydaje.

**NIEZWERYFIKOWANE:** nie bylo klienta do testu. `ip neigh dev enp1s0` pozostaje puste — po drugiej
stronie kabla nic sie nie odezwalo. Potwierdzeniem bedzie dopiero urzadzenie, ktore pobierze adres
z `192.168.2.1` i wyjdzie na zewnatrz.

**Trwalosc po reboocie nie byla ponownie testowana** w tym przebiegu. Jednostka jest `enabled`,
a jej `ExecStart` czyta `/etc/darkstar/host-guard.nft`, wiec po starcie zaladuje nowa pule — ale
to wniosek z konfiguracji, nie zmierzony fakt.

Zadne haslo nie zostalo zapisane.
